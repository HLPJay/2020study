智能指针： 
	帮助我们管理动态内存的。
	独有指针  unique_ptr ==>栈变量，把new的对象和栈绑定了测试是否释放，用类析构函数测试
					测试是否释放，用类析构函数测试
			
			只能有一个对象拥有所有权， move和reset
	
			auto ptr = make_unique<string>();
			测试，链表用智能指针
	共享指针   share_ptr ==>多个对象可以共享所有权
		可以查看引用计数 .use_count();
			可以赋值，引用计数会加，为0时会释放
			getShared()的问题，以及处理方案
	了解独有指针的实现：   ==》通过模板+栈对象  仿照实现，  
			如何实现赋值和拷贝的移动语义
			使用using起别名   为地址和指针命名
	了解共享指针的实现：
		
