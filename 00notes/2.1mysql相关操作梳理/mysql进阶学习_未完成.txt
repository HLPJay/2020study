mysql进阶：
	1：代码连接数据库
	2：事务==》参考实现代码，隔离机制（读写锁，一次性快照读MVCC）
	3：行级锁（共享锁和排他锁），表级锁（共享锁和排他锁），页级锁（锁定相邻的一组记录）（BDB支持）
		ＭySQL的表锁有两种模式： 
			表共享读锁（Table Read Lock） 
			表独占写锁（Table Write Lock） 
		InnoDB两种类型的行锁： 
			共享锁（S）：允许一个事务去读一行，阻止其他事务获得相同数据集的排他锁。 
			排他锁（Ｘ）：允许获取排他锁的事务更新数据，阻止其他事务取得相同的数据集共享读锁和排他写锁。 
		InnoDB两种内部使用的意向锁（Intention Locks），这两种意向锁都是表锁。 
			意向共享锁（IS）：事务打算给数据行共享锁，事务在给一个数据行加共享锁前必须先取得该表的IS锁。 
			意向排他锁（IX）：事务打算给数据行加排他锁，事务在给一个数据行加排他锁前必须先取得该表的IX锁。
		
		InnoDB 中MVCC的实现方式？
	4：索引  ===》源码认知    InnoDb，MyISAM的源码实现原理 B+树
				   mysql的运行机制
	5：存储引擎：
			查询当前存储引擎：
			查询默认存储引擎：
			查看表的存储引擎：
			新建表设置存储引擎：
			
			
			InnoDB： *.frm(存表结构) *.ibd（存数据，索引）
			MyISAM：*.frm(存表结构) *.MyD(存表数据)   *.MYI(存表索引)
			
			MyISAM:存储限制可达256TB,支持索引，表级锁定，数据压缩。 
			InnoDB：存储限制为64TB,支持事务和索引，锁颗粒为行锁，支持外键。
			
			MyISAM：主键索引的叶子节点只存放数据在物理磁盘上的指针； 
			InnoDB：所有的数据按照主键来组织。数据和索引放在一块，都位于B+数的叶子节点上；
			
		选择索引：
			
			1、使用场景是否需要事务支持； 
			2、是否需要支持外键； 
			3、是否需要支持高并发，InnoDB的并发度远高于MyISAM； 
			4、高效缓冲数据，InnDB对数据和索引都做了缓冲，而MyISAM只缓冲了索引；
	6：BinLog:记录日志
			
	7: 分库分表
	
	8：MYSL字符集：UTf-8(支持三个字节) 和uft8mb4（超过三个字节要用）
		
		
	9：apt-file 查找文件 apt-file search filename
	10：了解性能	
		Explain关键字模拟优化器执行SQL查询语句，知道如何运行，分析性能。
		慢查询日志：运行时间超过long_query_time 默认值，就会记录日志 ==》手动配置
	11：mysql定时任务
	12：存储过程 ==》sql语句集。
	13：触发器 ===》基于触发的
	14：视图 ===》虚拟表，查询定义
	15：复制表/临时表，MYSQL优化，MYSQL函数